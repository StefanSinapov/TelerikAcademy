// Generated by CoffeeScript 1.7.1
(function() {
  var follow, process, users;

  users = ['PhantomJS', 'ariyahidayat', 'detronizator', 'KDABQt', 'lfranchi', 'jonleighton', '_jamesmgreene', 'Vitalliumm'];

  follow = function(user, callback) {
    var page;
    page = require('webpage').create();
    return page.open('http://mobile.twitter.com/' + user, function(status) {
      var data;
      if (status === 'fail') {
        console.log(user + ': ?');
      } else {
        data = page.evaluate(function() {
          return document.querySelector('div.profile td.stat.stat-last div.statnum').innerText;
        });
        console.log(user + ': ' + data);
      }
      page.close();
      return callback.apply();
    });
  };

  process = function() {
    var user;
    if (users.length > 0) {
      user = users[0];
      users.splice(0, 1);
      return follow(user, process);
    } else {
      return phantom.exit();
    }
  };

  process();

}).call(this);

//# sourceMappingURL=follow.map
