// Generated by CoffeeScript 1.7.1
(function() {
  var scanDirectory, system;

  system = require('system');

  if (system.args.length !== 2) {
    console.log("Usage: phantomjs scandir.coffee DIRECTORY_TO_SCAN");
    phantom.exit(1);
  }

  scanDirectory = function(path) {
    var fs;
    fs = require('fs');
    if (fs.exists(path) && fs.isFile(path)) {
      return console.log(path);
    } else if (fs.isDirectory(path)) {
      return fs.list(path).forEach(function(e) {
        if (e !== "." && e !== "..") {
          return scanDirectory(path + "/" + e);
        }
      });
    }
  };

  scanDirectory(system.args[1]);

  phantom.exit();

}).call(this);

//# sourceMappingURL=scandir.map
